runtime: "container"
build:
  dockerfile: "Dockerfile"
  dockerBuildPath: "."
startCommand:
  type: "http"
  configSchema:
    type: object
    description: No configuration required for Clinical Trials MCP Server
  exampleConfig: {}
  
